package com.TicketViewer.View;

import org.zendesk.client.v2.model.Ticket;

import java.util.ArrayList;

public class TicketListDisplay {
    //attributes displayed : id, type, subject, priority, status, created_at, updated_at
    public static void display(ArrayList<Ticket> list, int currentPage, int totalPages){ //the list should contain 25 tickets or less
        System.out.println();
    }
}
